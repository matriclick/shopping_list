<h1>Listing measures</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @measures.each do |measure| %>
  <tr>
    <td><%= measure.name %></td>
    <td><%= measure.description %></td>
    <td><%= link_to 'Show', measure %></td>
    <td><%= link_to 'Edit', edit_measure_path(measure) %></td>
    <td><%= link_to 'Destroy', measure, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Measure', new_measure_path %> | <%= link_to 'Administrador', home_administrator_home_path %>
